{
  "name": "academic-task-manager-backend",
  "version": "1.0.0",
  "description": "Lambda functions for Academic Task Manager",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "deploy": "sam build && sam deploy"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1098.0",
    "@aws-sdk/client-secrets-manager": "^3.1098.0",
    "@aws-sdk/client-sns": "^3.1098.0",
    "@aws-sdk/lib-dynamodb": "^3.1098.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.162",
    "jest": "^30.4.2"
  }
}
